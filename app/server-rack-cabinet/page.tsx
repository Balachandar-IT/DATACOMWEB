import { LegacyStaticPage, legacyMetadata } from "../legacy-static-page";

export const metadata = legacyMetadata("server-rack-cabinet");

export default function ServerRackCabinetPage() {
  return <LegacyStaticPage slug="server-rack-cabinet" />;
}
